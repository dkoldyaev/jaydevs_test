export enum ErrorMessagesEnum {
  WRONG_PARAMS_COUNT = 'Wrong arguments. Use mpm run start --help to check syntax',
  WRONG_COMMAND = 'Wrong command',
}

export enum DefaultChangePrice {
  DELETE = 1,
  REPLACE = 1,
  INSERT = 1,
}